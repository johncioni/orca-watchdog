# Paths ineligible for the docs/test/size review skips (the only skips; see
# ~/.agents/MODELS.md). Every path handling auth/permissions, schema or
# migrations, outbound mail, or payments belongs here. Bash glob; '*' spans '/'.
# Keep in sync with CLAUDE.md "Review loop".
watchdog.mjs
install.sh
uninstall.sh
orca.yaml
scripts/orca-setup.sh
.github/*
CLAUDE.md
